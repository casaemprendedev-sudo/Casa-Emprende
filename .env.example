# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Drive Configuration - Service Account
# 1. Crear Service Account en Google Cloud Console
# 2. Descargar JSON de credenciales
# 3. Compartir carpeta raíz de Drive con el email del Service Account (como Editor)
# 4. Copiar el ID de la carpeta desde la URL

VITE_GOOGLE_DRIVE_FOLDER_ID=your-root-folder-id-here

# Backend URL (opcional, por defecto usa /api para Vercel)
# VITE_BACKEND_URL=http://localhost:3000/api

# Service Account JSON (solo para backend)
# Obtén este JSON desde Google Cloud Console
# Service Accounts → Create Service Account → Download JSON key
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"...",...}
